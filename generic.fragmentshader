#version 330
in vec2 UV;
flat in float terr;
uniform sampler2DArray TODOTestSampler;

out vec4 color;
void main(){
  uint terrainIndex = uint(terr);
  color = texture( TODOTestSampler, vec3(UV,terrainIndex) ).rgba;
}
